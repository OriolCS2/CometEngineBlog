<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Performance on Comet Engine Blog</title><link>https://OriolCS2.github.io/CometEngineBlog/tags/performance/</link><description>Recent content in Performance on Comet Engine Blog</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Wed, 23 Sep 2026 03:00:00 +0000</lastBuildDate><atom:link href="https://OriolCS2.github.io/CometEngineBlog/tags/performance/index.xml" rel="self" type="application/rss+xml"/><item><title>How Comet Draws a Frame</title><link>https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/</link><pubDate>Wed, 23 Sep 2026 03:00:00 +0000</pubDate><guid>https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/</guid><description>&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/cover.png" alt="Featured image of post How Comet Draws a Frame" /&gt;&lt;p&gt;Everything in a 2D scene is a textured rectangle. Every sprite, every tile, every letter of text, every particle. That sounds like it should make rendering easy, and in a way it does. Drawing one rectangle is trivial. Drawing four thousand of them without asking the GPU four thousand separate times is the part that takes work.&lt;/p&gt;
&lt;p&gt;This post follows one frame, from &amp;ldquo;here is a scene&amp;rdquo; to &amp;ldquo;here are the pixels&amp;rdquo;.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/scene.png"
width="952"
height="590"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/scene_hu_c61ca02a0a77abee.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/scene_hu_ca7735f62b5dce49.png 1024w"
loading="lazy"
alt="The demo scene I use through this post"
class="gallery-image"
data-flex-grow="161"
data-flex-basis="387px"
&gt;&lt;/p&gt;
&lt;h2 id="culling"&gt;Culling
&lt;/h2&gt;&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/viewport.png"
width="1124"
height="657"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/viewport_hu_20408680589c36d7.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/viewport_hu_6c49cb1f30839686.png 1024w"
loading="lazy"
alt="The camera viewport drawn in the Scene view"
class="gallery-image"
data-flex-grow="171"
data-flex-basis="410px"
&gt;&lt;/p&gt;
&lt;p&gt;That red rectangle in the Scene view is what the active camera can see. Everything outside it is thrown away before it reaches the GPU at all.&lt;/p&gt;
&lt;p&gt;Comet keeps a &lt;strong&gt;spatial index&lt;/strong&gt; of renderers, a structure that can answer &amp;ldquo;what overlaps this rectangle?&amp;rdquo; without testing every object in the scene. On a small scene this does not matter at all. On a level with fifteen thousand tiles where the camera is showing two hundred of them, it decides whether the level runs.&lt;/p&gt;
&lt;p&gt;The property I care about is that the cost of culling grows with what is visible, not with what exists. That is why a big Comet level does not get slower as you keep adding more level further away.&lt;/p&gt;
&lt;p&gt;There is a bug worth mentioning here, because it shows what happens when the index goes stale. Until 2.8, tiles you painted in the editor could vanish once their tilemap stopped being the active paint target. The tiles were there. The culling tree had not been rebuilt after the stroke, so the renderer believed there was nothing to draw.&lt;/p&gt;
&lt;h2 id="sorting"&gt;Sorting
&lt;/h2&gt;&lt;p&gt;2D rendering is mostly about order. There is no depth buffer sorting this out for you, so whatever is drawn last ends up on top.&lt;/p&gt;
&lt;p&gt;Comet uses three levels:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Sorting layer&lt;/strong&gt;. A named, project-wide list that you define in order. &lt;code&gt;Background&lt;/code&gt;, &lt;code&gt;Terrain&lt;/code&gt;, &lt;code&gt;Characters&lt;/code&gt;, &lt;code&gt;Effects&lt;/code&gt;, &lt;code&gt;UI&lt;/code&gt;. This is the coarse control and it is the one you should be using.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Order in layer&lt;/strong&gt;. An integer inside a layer, for &amp;ldquo;this specific thing goes in front of that specific thing&amp;rdquo;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Depth&lt;/strong&gt;. Position along Z, which resolves anything still tied.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;I prefer sorting layers over nudging Z values because a layer has a name. Six months later, &lt;code&gt;Characters&lt;/code&gt; still means something to me. &lt;code&gt;z = -0.03&lt;/code&gt; does not.&lt;/p&gt;
&lt;h2 id="batching"&gt;Batching
&lt;/h2&gt;&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/pipeline.png"
width="1100"
height="606"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/pipeline_hu_8693214841c8c14f.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/pipeline_hu_bbfc6af2ecbc91a4.png 1024w"
loading="lazy"
alt="The rendering pipeline, from culling to submission"
class="gallery-image"
data-flex-grow="181"
data-flex-basis="435px"
&gt;&lt;/p&gt;
&lt;p&gt;Comet walks the sorted list and merges consecutive renderers that can share a draw call into one. One vertex buffer, one texture bind, one submission.&lt;/p&gt;
&lt;p&gt;Fifty coins with the same sprite become one draw call. A tilemap of two thousand tiles from one tileset becomes a handful. This is the step that decides whether a 2D renderer is fast or slow.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/batch-breakers.png"
width="1100"
height="386"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/batch-breakers_hu_d86ff848bfe978b4.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/how-comet-draws-a-frame/batch-breakers_hu_e4aa748a61dd7c89.png 1024w"
loading="lazy"
alt="The three things that break a batch"
class="gallery-image"
data-flex-grow="284"
data-flex-basis="683px"
&gt;&lt;/p&gt;
&lt;p&gt;Three things break the merge:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;A different material.&lt;/strong&gt; A different shader, or different parameters, means a different GPU state.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;A different texture.&lt;/strong&gt; This is what sprite atlases are for. Pack twenty sprites onto one page and they can all batch together. Atlases get their own post in March.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Something drawn between them in the sort order.&lt;/strong&gt; This one catches everyone. Two sprites using the same material will not merge if a third sprite with a different material sorts between them, because merging them would move that third sprite to the wrong side of something.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;That last rule is why the practical advice is to keep things that look alike in the same sorting layer. You are not only organising the scene, you are giving the batcher something it can work with.&lt;/p&gt;
&lt;h2 id="handing-the-work-over"&gt;Handing the work over
&lt;/h2&gt;&lt;p&gt;The batched result becomes a &lt;strong&gt;command buffer&lt;/strong&gt;, a backend-agnostic list of &amp;ldquo;bind this, set that, draw this many&amp;rdquo;. Nothing in it mentions OpenGL or Vulkan.&lt;/p&gt;
&lt;p&gt;That buffer is packed into a &lt;strong&gt;frame packet&lt;/strong&gt; and handed to the render thread, which turns it into real API calls while the game thread has already moved on to the next frame. On every platform except the web, which has no threads at all, those two things really are running at the same time.&lt;/p&gt;
&lt;p&gt;That is the whole of next week&amp;rsquo;s post, so I will stop there.&lt;/p&gt;
&lt;h2 id="the-stats-panel"&gt;The Stats panel
&lt;/h2&gt;&lt;p&gt;The Game panel has a &lt;strong&gt;Stats&lt;/strong&gt; toggle. Turn it on and you get live rendering statistics: draw calls, batches, vertices, triangles and the frame time.&lt;/p&gt;
&lt;p&gt;Draw calls are the number I watch when I am optimising, and I find them much more useful than frame rate, because frame rate is capped and noisy while draw calls are exact. Add an atlas and watch the count fall. Add one sprite with a different material into the middle of a batched group and watch it jump.&lt;/p&gt;
&lt;p&gt;Some rough numbers from this project. The fifteen-entity scene above sits in single-digit draw calls. A test level with about four thousand sprites drawn from three atlases lands around twelve. A deliberately hostile version of the same level, where every sprite has its own material, gets you four thousand, and a frame time you can feel.&lt;/p&gt;
&lt;p&gt;In all three cases it is the same scene, on the same GPU, producing the same pixels. The only thing that changed is whether the renderer was allowed to group the work.&lt;/p&gt;
&lt;h2 id="what-comet-does-not-do"&gt;What Comet does not do
&lt;/h2&gt;&lt;p&gt;A few limits I should be honest about.&lt;/p&gt;
&lt;p&gt;There is no automatic atlasing at build time. If you want sprites to batch, you put them in a Sprite Atlas yourself. I have gone back and forth on this and right now I think the explicit version is better, because automatic packing turns the batching behaviour of your game into something that falls out of an algorithm you cannot see.&lt;/p&gt;
&lt;p&gt;There is no draw-call budget warning either. Nothing tells you that you have made things worse. The Stats panel will show you, if you look.&lt;/p&gt;
&lt;p&gt;And sorting is per-camera, per-frame, on the CPU. For the object counts a 2D game realistically has, I think that is the right trade. It is also why the GPU particle path, which can have a hundred thousand particles in one system, does its sorting in a completely different way.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;Next Wednesday: the same frame, drawn twice. Comet renders through an abstraction with an OpenGL backend and a Vulkan backend, picks between them at runtime, and hands the result to a render thread. I will talk about why I put Vulkan in a 2D engine, and what happens on the one platform that has no threads.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Comments and questions welcome ;)&lt;/em&gt;&lt;/p&gt;</description></item><item><title>Loading a World</title><link>https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/</link><pubDate>Wed, 16 Sep 2026 03:00:00 +0000</pubDate><guid>https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/</guid><description>&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/cover.png" alt="Featured image of post Loading a World" /&gt;&lt;p&gt;A scene is the file that holds a piece of your game. Opening one, saving one, and loading one while another is already running are three problems of very different sizes, and this post is about all three.&lt;/p&gt;
&lt;h2 id="what-is-actually-in-there"&gt;What is actually in there
&lt;/h2&gt;&lt;p&gt;Comet scenes in the editor are JSON. Not a custom binary format that you need a tool to read, but actual JSON, on disk, in your &lt;code&gt;Assets/&lt;/code&gt; folder, which you can open in a text editor and diff in git.&lt;/p&gt;
&lt;p&gt;This is the top of the scene I have been using for these posts.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/scene-json.png"
width="1000"
height="690"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/scene-json_hu_a6dd2c74c8afa614.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/scene-json_hu_44756907bd322774.png 1024w"
loading="lazy"
alt="The first entities of a Comet scene file opened as JSON"
class="gallery-image"
data-flex-grow="144"
data-flex-basis="347px"
&gt;&lt;/p&gt;
&lt;p&gt;Every entity is a record: its name, its ID, its parent&amp;rsquo;s ID, its enabled state, its layer and tag, and then a dictionary of behaviours keyed by their own IDs. Each behaviour writes whatever it needs. A Transform writes position, rotation and scale, a Camera writes its culling mask and clear colour, a Sprite Renderer writes the sprite it points at and its sorting layer.&lt;/p&gt;
&lt;p&gt;Two things are worth noticing.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Everything is an ID, not a name and not a path.&lt;/strong&gt; &lt;code&gt;ParentID&lt;/code&gt; is a number. When a script field references another entity, that is a number too. Names are for humans, and the engine never uses them to find anything, which is why renaming an entity can never break a reference.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Behaviours are a dictionary, not an array.&lt;/strong&gt; Each behaviour has its own persistent ID, so adding, removing or reordering behaviours does not shift what anything else points at.&lt;/p&gt;
&lt;p&gt;That readability is a deliberate editor only choice. In a &lt;strong&gt;build&lt;/strong&gt;, this same scene becomes a binary blob, an index of &lt;code&gt;ID + Hash + Type + Offset&lt;/code&gt; entries followed by packed data. The editor wants a format you can inspect and merge, and the runtime wants a format that loads without parsing thirty thousand characters of text. There is no reason those have to be the same format, so they are not.&lt;/p&gt;
&lt;h2 id="putting-it-back-together-in-order"&gt;Putting it back together, in order
&lt;/h2&gt;&lt;p&gt;This part is delicate.&lt;/p&gt;
&lt;p&gt;You cannot just walk the entity list and construct things as you meet them. Think of a scene where a rigid body has a collider, and a joint connects that body to a second one that appears &lt;strong&gt;later&lt;/strong&gt; in the file. Construct the joint when you reach it and you are connecting to a body that does not exist yet.&lt;/p&gt;
&lt;p&gt;So loading happens in stages.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/load-order.png"
width="1100"
height="716"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/load-order_hu_f7c1ded563fb224d.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/load-order_hu_2082562dc2781f74.png 1024w"
loading="lazy"
alt="The load stages, from empty entities through physics to cross references"
class="gallery-image"
data-flex-grow="153"
data-flex-basis="368px"
&gt;&lt;/p&gt;
&lt;p&gt;First every entity and every behaviour is created, present but empty. Then rigid bodies enter the physics world. Then colliders attach to bodies that are now guaranteed to exist. Then joints connect bodies that now have shapes. Then everything else resolves its cross references, and by that point every object in the scene is available to be pointed at.&lt;/p&gt;
&lt;p&gt;Each stage depends on the one above it having completely finished. This is invisible when it works and produces very confusing bugs when it does not, like a joint that behaves oddly only when you saved the scene with the entities in a particular order.&lt;/p&gt;
&lt;p&gt;Two of the bugs fixed in 2.8 live exactly here: joints that ended up connecting a body to itself and corrupted the physics world, and a rigid body attached to a joint crashing when you dragged it with the scene gizmos.&lt;/p&gt;
&lt;h2 id="three-ways-to-load"&gt;Three ways to load
&lt;/h2&gt;&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/load-modes.png"
width="1100"
height="496"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/load-modes_hu_d0b9b696135f6892.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/loading-a-world/load-modes_hu_56f2f204dd103a0c.png 1024w"
loading="lazy"
alt="The three scene load modes and when each one runs"
class="gallery-image"
data-flex-grow="221"
data-flex-basis="532px"
&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;LoadSceneInstantly&lt;/code&gt;&lt;/strong&gt; does what it says. The current frame stops, the scene is torn down, the new one is built, and then the frame continues. It is the simplest one and it is right for small scenes and editor tooling.&lt;/p&gt;
&lt;p&gt;It is also the one that will hurt you, for a reason that is not obvious. Calling it from inside a collision callback means you are destroying the world in the middle of the physics step that is iterating over it. Comet defends against a lot of this, but &amp;ldquo;destroy everything, right now, from inside a callback&amp;rdquo; is a hard thing to make safe.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;LoadSceneNextFrame&lt;/code&gt;&lt;/strong&gt; is the same operation, deferred until the current frame has finished cleanly. This is the correct default and it is what I use for essentially every scene transition. Nothing is mid iteration, the frame ends, and then the swap happens.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;LoadSceneAsync&lt;/code&gt;&lt;/strong&gt; streams the new scene in the background while the current one keeps running and rendering. This is what a loading screen is built on: start the load, keep updating your spinner, poll for completion, then swap. 2.8 improved both async asset and async scene loading noticeably, and also the plain large scene load path.&lt;/p&gt;
&lt;p&gt;You can also load a scene &lt;strong&gt;by ID&lt;/strong&gt; rather than by reference. That sounds like a small thing, but it is what makes a data driven level flow possible, because a script can hold a list of scene IDs and load whichever one the save file says you were in.&lt;/p&gt;
&lt;h2 id="leaving-play-mode-is-also-a-scene-load"&gt;Leaving play mode is also a scene load
&lt;/h2&gt;&lt;p&gt;This one took me an embarrassingly long time to make fast.&lt;/p&gt;
&lt;p&gt;When you press play in the editor, Comet has to remember the scene exactly as it was, because when you press stop it must put it back. That is the whole contract of play mode. Every change your scripts made during play has to be undone.&lt;/p&gt;
&lt;p&gt;For a long time the recovery on a big scene took several seconds. You would press stop and just sit there waiting. 2.8 got that down to a fraction of a second, and it is one of the changes that has most improved how the editor feels to use, even though it is completely invisible in a feature list.&lt;/p&gt;
&lt;p&gt;A related detail, also fixed recently: delta time was not being normalised after a scene load, so the first frame of a new scene received an enormous delta, the accumulated time of the whole load. Anything integrating over delta time on that first frame took one gigantic step. Objects teleported. It looked like a physics bug for a long time before I thought to look at the clock.&lt;/p&gt;
&lt;h2 id="scenes-as-assets"&gt;Scenes as assets
&lt;/h2&gt;&lt;p&gt;A scene is a resource like any other, with an ID, which means other things can reference it. A script can expose a &lt;code&gt;Scene&lt;/code&gt; field and you drag one in from the Project panel.&lt;/p&gt;
&lt;p&gt;That matters at build time. Scenes referenced from a script or a CometObject are &lt;strong&gt;automatically included in the build&lt;/strong&gt;. You do not maintain a list of levels somewhere and remember to update it. If something in your project points at a scene, that scene ships.&lt;/p&gt;
&lt;p&gt;The build also does the reverse and strips out assets that nothing references and that have no content group assigned. That is a subject for its own post, in May.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;Next Wednesday we follow one frame all the way to the screen: sorting layers, the spatial index that throws away everything off camera, what merges into a single draw call and what quietly refuses to.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Comments and questions welcome ;)&lt;/em&gt;&lt;/p&gt;</description></item><item><title>Saying Goodbye to C# (Part 2): What I Got Back</title><link>https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/</link><pubDate>Wed, 12 Aug 2026 03:00:00 +0000</pubDate><guid>https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/</guid><description>&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/cover.png" alt="Featured image of post Saying Goodbye to C# (Part 2): What I Got Back" /&gt;&lt;p&gt;&lt;a class="link" href="https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-how-the-migration-actually-went-part-1/" &gt;Last Wednesday&lt;/a&gt; was the bill. Nine months, 300 files, no generics, no reflection, no garbage collector, and five months of a console that was nothing but red.&lt;/p&gt;
&lt;p&gt;This is the part where I explain why I would do it again.&lt;/p&gt;
&lt;h2 id="one-binary"&gt;One binary
&lt;/h2&gt;&lt;p&gt;This is the whole thing, really. Everything else in this post is a consequence of it.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/one-binary.png"
width="1100"
height="386"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/one-binary_hu_8bfc09cffa8f30d8.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/one-binary_hu_44ca944c594359b3.png 1024w"
loading="lazy"
alt="One binary"
class="gallery-image"
data-flex-grow="284"
data-flex-basis="683px"
&gt;&lt;/p&gt;
&lt;p&gt;With Mono, a Comet game was three things that had to agree with each other: the engine, the Mono runtime, and your compiled assembly. Shipping to a platform meant getting all three of them onto it, in versions that were compatible, at a size that was acceptable.&lt;/p&gt;
&lt;p&gt;With AngelScript there is no second runtime. The compiler and the virtual machine are a C++ library that builds as part of the engine, the same way Box2D or SoLoud does. Your scripts compile to bytecode that lives inside the project. When Comet builds for Android, the scripting system is simply already there, because it was compiled into the engine binary along with everything else.&lt;/p&gt;
&lt;p&gt;The practical consequence is that adding a platform stopped being a scripting problem. Web and Android landed in 2.0, the same release that removed C#, and that is not a coincidence, it is cause and effect.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/platforms.png"
width="1100"
height="300"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/platforms_hu_88159a07842d69a2.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/platforms_hu_5d0e682afda8f4e7.png 1024w"
loading="lazy"
alt="Platforms unlocked"
class="gallery-image"
data-flex-grow="366"
data-flex-basis="880px"
&gt;&lt;/p&gt;
&lt;h2 id="compile-times-and-how-they-changed-my-evenings"&gt;Compile times, and how they changed my evenings
&lt;/h2&gt;&lt;p&gt;I did not expect this one to matter as much as it does.&lt;/p&gt;
&lt;p&gt;Compiling the C# layer meant invoking Roslyn, producing an assembly and reloading it. It was not slow in absolute terms, a few seconds, but a few seconds is exactly the length of time that makes you stop and check something else, and then you have lost the thread.&lt;/p&gt;
&lt;p&gt;AngelScript compiles the whole script project in well under a second on this machine. It sounds like a minor quality of life detail, and it is not. It changed the way I write gameplay code, because the loop is now short enough that trying something beats reasoning about it. I make more small changes and fewer big ones.&lt;/p&gt;
&lt;h2 id="what-the-compiler-started-catching"&gt;What the compiler started catching
&lt;/h2&gt;&lt;p&gt;C# is statically typed too, so I was not expecting a difference here. There was one anyway, and it comes from AngelScript being stricter about the things C# is relaxed about.&lt;/p&gt;
&lt;p&gt;&lt;img src="https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/what-typing-caught.png"
width="1100"
height="386"
srcset="https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/what-typing-caught_hu_72e7e376a9b71edd.png 480w, https://OriolCS2.github.io/CometEngineBlog/p/saying-goodbye-to-csharp-part-2-what-i-got-back/what-typing-caught_hu_aa7ce42839b2075b.png 1024w"
loading="lazy"
alt="What typing caught"
class="gallery-image"
data-flex-grow="284"
data-flex-basis="683px"
&gt;&lt;/p&gt;
&lt;p&gt;Implicit numeric conversion is the big one. C# will quietly widen an &lt;code&gt;int&lt;/code&gt; to a &lt;code&gt;float&lt;/code&gt; and let you carry on. AngelScript is far less willing, and while porting I found a genuine bug in the animation system that had been sitting there for over a year: a frame index doing integer division where it should have been float, producing a value that was correct for most frame counts and subtly wrong for others. It had shipped. Nobody had reported it, because it looked like an animation being slightly off rather than a number being wrong.&lt;/p&gt;
&lt;p&gt;I want to be clear about the limit of all this. &lt;strong&gt;Type safety does not solve object lifetime.&lt;/strong&gt; A reference to a destroyed Entity is still type correct. That problem needed its own answer, which is why Comet has &lt;code&gt;IsValid()&lt;/code&gt; instead of letting you write &lt;code&gt;!= null&lt;/code&gt;, because a plain null check really does return the wrong answer in some teardown situations. There is a whole post about object lifetime later in the year.&lt;/p&gt;
&lt;h2 id="what-i-actually-miss"&gt;What I actually miss
&lt;/h2&gt;&lt;p&gt;Three things, and I would take the trade again knowing all three.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;LINQ.&lt;/strong&gt; Not the clever parts, just &lt;code&gt;.Where()&lt;/code&gt; and &lt;code&gt;.Select()&lt;/code&gt; on a collection. I write more loops now than I used to.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Real generics.&lt;/strong&gt; I covered this last week. &lt;code&gt;array&amp;lt;T&amp;gt;&lt;/code&gt; plus hand registered container types is a workable answer, and 2.8 filling in &lt;code&gt;list&lt;/code&gt;, &lt;code&gt;stack&lt;/code&gt;, &lt;code&gt;queue&lt;/code&gt; and &lt;code&gt;set&lt;/code&gt; closed most of the gap, but &amp;ldquo;I need a generic helper&amp;rdquo; is still a conversation with the engine rather than something I just write.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;The tooling ecosystem.&lt;/strong&gt; When you write C#, an enormous amount of editor intelligence comes for free from tools other people built. With AngelScript, if I want autocomplete that understands my project, find all references, rename symbol, or a debugger, &lt;strong&gt;I have to write it&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;So I did. That turned out to be one of the better things to come out of this whole migration, and it gets two posts of its own in January.&lt;/p&gt;
&lt;h2 id="was-it-worth-it"&gt;Was it worth it?
&lt;/h2&gt;&lt;p&gt;The migration cost about nine months of evenings and it broke every existing project. In exchange, Comet gained two platforms it could not otherwise have had, a script compile loop fast enough to change how I work, a type system strict enough to find a bug I had already shipped, and a scripting layer with no external runtime to keep alive for the rest of the engine&amp;rsquo;s life.&lt;/p&gt;
&lt;p&gt;The thing I did not appreciate in May 2025 is that the last one is the real prize. Mono was not only a portability problem, it was a permanent portability problem, one that comes back with every new platform and every runtime update, forever. Removing it did not only unlock web and Android. It removed an entire category of future work.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;Next Wednesday, something much lighter and a lot more visual: a proper tour of the editor. What every panel is for, why the layout is shaped the way it is, and a few small touches that took far longer to build than they look.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Comments and questions welcome ;)&lt;/em&gt;&lt;/p&gt;</description></item></channel></rss>